<h1>Subject</h1>
<p>Find me in app/views/subject/index.html.erb</p>
<p>With this table, you can view all currently enter subject and quickly view or search for any number of variable values. This table automatically sorts itself as you do so.</p>
<div>
  <table id="subject_test" cellpadding="0" cellspacing="0" border="0" class="table table-bordered table-striped">
    <thead>
      <tr>
        <th>Subject ID</th>
        <th>Study Site</th>
        <th>Edit</th>
        <th>Screening Log</th>
        <th>Baseline</th>
        <th>Treatment Completion</th>
        <th>3 Week Follow Up</th>
        <th>6 Week Follow Up</th>
        <th>18 Week Follow Up</th>
        <th>6 Month Follow Up</th>
        <th>1 Year Follow Up</th>
        <th>Treatment Group</th>
      </tr>
    </thead> 
    <tbody>
      <% @subjects.each do |sub| %>
        <tr>
          <td><%= sub.subject_id %></td>
          <td><%= sub.study_site == 1? "Seattle" : sub.study_site == 2? "Portland" : "nil"%></td>
          <td><%= link_to "edit", edit_subject_path(sub) %></td>
          <td><%= link_to "edit", subject_screening_path(sub) %></td>
          <td><%= link_to "edit", subject_baseline_path(sub) %></td>
          <td><%= link_to "edit", subject_tc_path(sub) %></td>
          <td><%= link_to "edit", subject_fu3week_path(sub) %></td>
          <td><%= link_to "edit", subject_fu6week_path(sub) %></td>
          <td><%= link_to "edit", subject_fu18week_path(sub) %></td>
          <td><%= link_to "edit", subject_fu6month_path(sub) %></td>
          <td><%= link_to "edit", subject_fu1year_path(sub) %></td>
          <td><%= sub.treatment_group %></td>
        </tr> 
      <% end %>
    </tbody>
  </table>
<div>