EDC:

User Authentication
- Default all permissions to null.
- Figure out what roles are allowed to see which pages.

- Add authentication to every page

- Make sure subjects are saved to groups, behind the scenes?